# slugcmplr-fixture-binary

A fixture for `slugcmplr` with an embeded binary, useful for less cluttered build output.

Binary is this precompiled `cd src; GOOS=linux GOARCH=amd64 go build -o ../server .`
